My Plans:

First of all, need to figure out how the game works (save file etc)
Then somehow need to create a server what can sync the save to the other clients, but all of the clients have their own progress / tasks. 
Saves structure:
GameX (save folder)
-> dbreadcompleted (todo)
-> dbreadinitiated (todo)
-> GGEUserData.dat (todo)
-> lastSave.pld (todo)
-> NAME.spo (todo) (planet itself)
-> planetRecords.pkp (todo)
-> planetRecords.pkt (todo)
-> planetRecords.pkt (todo)
-> starts (todo)

found this: https://github.com/lingeringwillx/Refpack-QFS-Resources
